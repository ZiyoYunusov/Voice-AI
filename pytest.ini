[pytest]
asyncio_mode = auto
# Без этого голый `pytest` (в отличие от `python -m pytest`) не добавляет
# корень репозитория в sys.path, и импорт `import tools` в тестах падает
# с ModuleNotFoundError — воспроизводится в CI, где вызывается именно `pytest -v`.
pythonpath = .
